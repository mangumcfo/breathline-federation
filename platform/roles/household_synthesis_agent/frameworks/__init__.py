"""Household synthesis frameworks.

Reuses the synthesis framework from synthesis_agent.frameworks.synthesis —
the orchestration logic does not need narrowing; the narrowing happens
at the role-handler level (HouseholdSynthesisAgent.process).
"""
